

const About = () => {
    return (
        <div>
            <p>You'll now see the root layout again but a blank page on the right. We need to tell the root route where we want it to render its child routes. We do that with</p>
        </div>
    );
};

export default About;