

const FeaturedJobs = () => {
    return (
        <div className="text-center font-bold">
            <h1 className="text-5xl">Featured Jobs</h1>
            <p>doing a full document request for the next URL instead of using React Router.</p>
        </div>
    );
};

export default FeaturedJobs;